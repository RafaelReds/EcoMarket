<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="d-flex justify-content-center align-items-center min-vh-100">

  <div class="container w-50">
    <h1 class="mb-4 text-center">EcoMarket</h1>

       <% if (user) { %>
      <div class="alert alert-success text-center">
        Bienvenido, <strong><%= user.nombre %></strong> (Rol: <%= user.rol %>)
      </div>
    
      <div class="d-flex flex-column align-items-center">
        <% if (user.rol === 'cliente') { %>
          <a href="/productos" class="btn btn-eco mb-2 w-100">Ver catálogo</a>
          <a href="/historial" class="btn btn-outline-secondary mb-2 w-100">Historial de compras</a>
        <% } %>
    
        <% if (user.rol === 'productor') { %>
          <a href="/productor/productos" class="btn btn-eco mb-2 w-100">Mis productos</a>
          <a href="/productor/pedidos" class="btn btn-outline-secondary mb-2 w-100">Pedidos recibidos</a>
        <% } %>
    
        <a href="/logout" class="btn btn-danger mt-2 w-100">Cerrar sesión</a>
      </div>
    <% } else { %>
      <div class="alert alert-warning text-center">No has iniciado sesión.</div>
      <div class="d-flex flex-column align-items-center">
        <a href="/login" class="btn btn-primary mb-2 w-100">Iniciar sesión</a>
        <a href="/registro" class="btn btn-secondary mb-2 w-100">Registrarse</a>
        <a href="/productos" class="btn btn-eco mt-2 w-100">Ver catálogo como visitante</a>
      </div>
    <% } %>
  </div>
</body>

</html>
