<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
</head>
<body>

  <%- include('partials/navbar') %>

  <div class="container">
    <h1 class="mb-4"><i class="fa-solid fa-box-archive"></i> Pedidos Recibidos</h1>
    

    <% if (pedidos.length === 0) { %>
      <div class="alert alert-info">Aún no has recibido pedidos.</div>
    <% } else { %>
      <% pedidos.forEach(pedido => { %>
  <div class="card mb-4 border-eco">
    <div class="card-header bg-light d-flex justify-content-between align-items-center">
      <div>
        <strong>Pedido #<%= pedido.id %></strong> – <%= pedido.fecha %>
      </div>
      <span class="badge 
        <% if (pedido.estado === 'pendiente') { %> bg-warning text-dark 
        <% } else if (pedido.estado === 'en proceso') { %> bg-info 
        <% } else { %> bg-success <% } %>">
        <%= pedido.estado.toUpperCase() %>
      </span>
    </div>

    <div class="card-body border-eco">
      <p><strong>Cliente:</strong> <%= pedido.cliente %></p>

      <ul class="list-group mb-3">
        <% pedido.productos.forEach(prod => { %>
          <li class="list-group-item d-flex flex-column flex-md-row justify-content-between align-items-md-center">
            <div class="mb-2 mb-md-0">
              <strong><%= prod.nombre %></strong> (x<%= prod.cantidad %>)<br>
              $<%= (prod.precio * prod.cantidad).toFixed(2) %>
            </div>

            <form action="/productor/pedidos/estado-item/<%= pedido.id %>/<%= prod.id %>" method="POST" class="d-flex align-items-center gap-2">
              <label for="estado_<%= prod.id %>" class="me-2 mb-0">Estado:</label>
              <select name="estado_envio" id="estado_<%= prod.id %>" class="form-select form-select-sm" onchange="this.form.submit()">
                <option value="pendiente" <%= prod.estado_envio === 'pendiente' ? 'selected' : '' %>>Pendiente</option>
                <option value="enviado" <%= prod.estado_envio === 'enviado' ? 'selected' : '' %>>Enviado</option>
                <option value="entregado" <%= prod.estado_envio === 'entregado' ? 'selected' : '' %>>Entregado</option>
              </select>
            </form>
          </li>
        <% }) %>
      </ul>
    </div>
  </div>
<% }) %>
<% } %>

  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
